# 開発用テストボタン一覧

開発中の動作確認のために設置している UI と、暫定（仮実装）のまま残っている操作をまとめる。
**リリース前に「撤去チェックリスト」を必ず消化すること。**

`__DEV__` 限定のものは Expo の開発ビルドでのみ表示され、本番ビルドには出力されない。

---

開発用のボタンはホーム画面の下部に「開発用パネル」（`src/app/index.tsx` の `DevPanel`）としてまとめている。

---

## 1. データ初期化（開発用パネル）

| 項目 | 内容 |
| :---- | :---- |
| 表示条件 | `__DEV__` のみ |
| 場所 | ホーム画面 下部・開発用パネル内「開発用: データ初期化して初期設定へ」 |
| 挙動 | 全ユーザーデータを削除 → 設定を再読込 → 初期設定画面（`/setup`）へ遷移 |
| 実装 | `src/app/index.tsx` の `DevPanel` / `src/db/repositories/maintenanceRepo.ts` の `resetUserData()` |

**補足**

- 削除は `DELETE FROM user` のみ。スキーマの `ON DELETE CASCADE` により、ユーザーに紐づく全データ（学習記録・進捗・各種設定・マイタグ・計測状態等）が連動削除される。マスタデータは残る
- 初期設定フローを何度も試すために用意したもの
- 正式なデータ初期化（要件10.10・確認ダイアログ・タイマー稼働中の禁止を含む）は **Phase 6** で設定画面に実装する。その際に本ボタンは撤去してよい

---

## 2. Lv切り替え（開発用パネル）

| 項目 | 内容 |
| :---- | :---- |
| 表示条件 | `__DEV__` のみ |
| 場所 | ホーム画面 下部・開発用パネル内「Lv切り替え」 |
| 挙動 | **Lv5 プレビュー ⇄ 実際のレベル** をトグル。背景アートも連動して切り替わる（`lv5.png` ⇄ 実レベルの画像） |
| 実装 | `src/app/index.tsx` の `devLevelOverride` と `DevPanel` |

**補足**

- **DBは一切変更しない**（表示上のプレビューのみ）
- 目的: レベルに応じて育つ灯りの発光の確認と、**明るい Lv5 背景の上でも灯りが視認できるか**の確認
- 灯りの発光の調整値は `src/components/level-badge.tsx` 冒頭の定数（`CORE_OPACITY_*` / `HALO_*` / `SHADOW_*`）
- 将来 Lv1〜5 の灯り画像素材へ差し替える予定のため、その際に本プレビューの扱いを見直す

---

## 4. おやすみボタン（ホーム画面・右側の月アイコン）※仮実装

| 項目 | 内容 |
| :---- | :---- |
| 表示条件 | ボタン自体は常時表示（本来のUI）。**仮ダイアログは `__DEV__` のみ** |
| 場所 | ホーム画面 右側のアイコン列（月） |
| 挙動 | 開発時: 仮のダイアログを表示する / 本番: 何も起きない |
| 実装 | `src/app/index.tsx` の `SideIcons` 内 `handleGoodnight` |

**補足**

- **Phase 7**（音の実装後）で要件13章のおやすみ機能に置き換える。確認表示 → BGM・環境音のフェードアウト → 暗転＋NPCおやすみメッセージ → タップで復帰

---

## 5. BGMミニプレイヤー（ホーム画面・右下）※仮実装

| 項目 | 内容 |
| :---- | :---- |
| 表示条件 | 常時表示（本来のUI）。BGM音量0のときは非表示（要件9） |
| 場所 | ホーム画面 右下（曲名・アーティスト名＋巻き戻し／再生・一時停止／スキップ） |
| 挙動 | 曲名・クレジットの表示は本物（`ambient_sound` マスタから取得）。**操作ボタンは押しても音は鳴らない**（再生・一時停止の見た目のみ切り替わる） |
| 実装 | `src/components/bgm-mini-player.tsx` |

**補足**

- 表示中の曲は BGMプールの先頭で固定。**シャッフル再生・自動再生（フェードイン）・長い曲名のスクロール表示は Phase 7** で実装する
- 音量0での非表示は起動時の値のみ参照している。音量変更への追従は Phase 7（AudioContext 実装時）に対応する

---

## 6. ダミー学習記録（開発用パネル）

| 項目 | 内容 |
| :---- | :---- |
| 表示条件 | `__DEV__` のみ |
| 場所 | ホーム画面 下部・開発用パネル内「学習記録+35分」「今夜の記録を消す」 |
| 挙動 | 「+35分」: 当学習日に実績35分のダミーセッションを1件追加する。合計が一日の目標時間に達したら達成記録（`daily_goal_achievement`）も残す / 「消す」: 当学習日のダミー記録と達成記録を削除する |
| 実装 | `src/app/index.tsx` の `DevPanel` / `src/db/repositories/devSeedRepo.ts` |

**補足**

- 目的: **本物の学習記録を作れるのは Phase 3（タイマー）から**であり、それまでホーム画面の「今夜の学習時間・目標達成状況」（要件2.1）を実機で確認できないため
- 35分は既定の目標60分に**2回で届く**値。1回目=未達成、2回目=達成、の両方の表示を確認できる
- **経験値・レベルの反映は行わない**（成長判定は Phase 4）。作るのは `study_session` と達成記録のみ
- `devSeedRepo.ts` は **Phase 3 でファイルごと削除する**（バレル `src/db/repositories/index.ts` の export も併せて外す）

---

## 7. 時刻の上書き・時刻を進める（開発用パネル）

| 項目 | 内容 |
| :---- | :---- |
| 表示条件 | `__DEV__` のみ |
| 場所 | ホーム画面 下部・開発用パネル内「時刻: 実時間 / 21:00 / 12:00」 |
| 挙動 | 押すたびに **実時間 → 21:00 → 12:00 → 実時間** と循環する。上書き中は時計の針・日時表示・**夜間帯判定（要件2.3）**・**学習日の集計**・**タイマーの計測と5:00判定**のすべてがその時刻で動く |
| 実装 | `src/lib/clock.ts`（時刻の実体）／ `src/app/index.tsx` の `DEV_CLOCK_HOURS` と `DevPanel`（切替UI） |

**「時刻を+30分進める」**

| 項目 | 内容 |
| :---- | :---- |
| 表示条件 | `__DEV__` のみ |
| 場所 | ホーム画面 下部・開発用パネル内「時刻を+30分進める」 |
| 挙動 | 押すたびにアプリ内の時刻を30分進める（`clock.ts` の `advanceDevTime`）。「時刻: 実時間」に戻すとオフセットは解除される |
| 目的 | **5:00自動終了**・**ポモドーロのフェーズ進行と全ループ完了**を、実際にその時刻まで待たずに確認するため |

**補足**

- 目的: 夜間帯（18:00〜翌5:00）の**内側と外側の両方**を、実際にその時刻まで待たずに確認するため
  - **21:00** = 夜間帯内 → 時計（学習開始ボタン）が**活性**
  - **12:00** = 夜間帯外 → 時計が**非活性**＋「この街が目覚めるのは 18:00 から」を表示
- **端末の時計は変更しない**（アプリが表示・判定に使う時刻だけを差し替える）
- 実体は `src/lib/clock.ts` の `nowMs()` に集約してある。アプリ内で現在時刻を使う箇所は**すべてここを経由する**ため、表示と判定が食い違わない
- 上書きは**実時間からのオフセット**として保持するため、合わせた後も時刻は自然に進む（21:00に合わせた5分後は21:05）
- **日付は実際の日付のまま**、時刻だけを合わせる。日付をまたぐ深夜帯（0:00〜5:00に前日へ帰属する動き）の確認には使えない
- 時刻を進めると計測中の実績学習時間も進む（時刻差分方式のため）。5:00を跨がせれば自動終了、ポモドーロの全ループ分を跨がせれば完了による自動終了を確認できる
- **オフセットはメモリ上にあり、アプリを再起動すると実時間へ戻る。** そのため**中断からの復元（要件3.2）の確認には使えない**（偽の時刻で開始したセッションを再起動すると、現在時刻が実時間へ戻り、経過が負→0に丸められて必ず「1分未満」で破棄されてしまう）。復元の確認は夜間帯に実時間で行うこと

---

## 撤去チェックリスト

- [x] **Phase 3**: 右上の時計タップの仮ダイアログを、タイマー設定モーダル（S3）＋夜間帯判定に置き換える（P3-1 で対応済み）
- [ ] **Phase 3**: ダミー学習記録のボタンと `src/db/repositories/devSeedRepo.ts` を撤去する（P3-5 完了時）
- [ ] **リリース前**: 時刻の上書きボタンを撤去する（`__DEV__` 限定のため本番には出ないが、コードは残る）
- [ ] **Phase 7**: おやすみボタンの仮ダイアログを、おやすみ機能（要件13章）に置き換える
- [ ] **Phase 7**: BGMミニプレイヤーを実再生（AudioContext + expo-audio）に接続する
- [ ] **Phase 6**: 設定画面のデータ初期化（要件10.10）を実装したら、開発用リセットボタン（`DevResetButton`）を撤去する
- [ ] 灯り画像（Lv1〜5）を導入したら、レベルプレビューの扱いを見直す
- [ ] リリース前に本ファイルの項目がすべて解消されているか確認する
