# テスト仕様書

> **このファイルは `npm run test:doc` で自動生成される。手で編集しても次回の生成で失われる。**
> 内容を変えたい場合は、テストコード（`src/lib/__tests__/`）か生成スクリプト（`scripts/generate-test-doc.js`）を編集すること。

## 1. 目的

本書は Chill Night Town の自動テストの内容と実行結果をまとめたものである。

テストの対象は **画面を見ても正しさが判断できず、壊れると学習記録・街の成長判定に直接影響する処理** に絞っている。
具体的には学習日の帰属、タイマーの計測、入力値の検証の3つである。
これらは「動いているように見えて実は間違っている」ことが起こり得るため、実行して確認できる形で検証手段を残す。

## 2. 対象範囲

| 区分 | 対象 | 検証方法 |
| :---- | :---- | :---- |
| テストを書く | 学習日の帰属（`src/lib/study-day.ts`） | 本書のテストケース |
| テストを書く | タイマー計測（`src/lib/timer.ts`） | 本書のテストケース |
| テストを書く | 入力値の検証（`src/lib/validation.ts`） | 本書のテストケース |
| テストを書かない | 画面（UI）・レイアウト・操作感 | 実機での目視確認 |
| テストを書かない | DBアクセス（リポジトリ層）・SQLの制約 | sqlite3 で実際にDBを作成して手動検証 |
| テストを書かない | Context（状態管理） | 実機での目視確認 |

画面・DBアクセス・Contextを自動テストの対象外としたのは、MVPの工数配分としてUIテストの整備が見合わないと判断したためである。
これらは実機確認とsqlite3による手動検証で担保する。

## 3. 実行方法と環境

```
npm test          # テストを実行する
npm run test:doc  # テストを実行し、本書を再生成する
```

| 項目 | 値 |
| :---- | :---- |
| 実行日時 | 2026/07/17 11:25 |
| テストフレームワーク | jest ~29.7.0 / jest-expo ~54.0.17 |
| Node.js | v24.18.0 |
| 実行環境 | win32 |

## 4. テストケース一覧

「検証観点」はテストコードの `describe`、「期待する挙動」は `it` に対応する。

### 4.1 学習日の帰属・夜間帯の判定

- **対象**: `src/lib/study-day.ts`
- **関連する仕様**: 要件0章（用語定義）／要件2.3（学習開始）
- **テストファイル**: `src/lib/__tests__/study-day.test.ts`

**なぜ検証するのか**: 「学習日 = 18:00〜翌5:00のサイクル、セッションの開始時刻が属するサイクルの開始日に帰属する」という規則は、このアプリで最も間違えやすい。カレンダー表示・目標達成判定・経験値付与がすべてこの結果に乗るため、日付・月・年をまたぐケースを固定する。

#### getStudyDate（学習日の帰属）

| No | 期待する挙動 | 結果 |
| :---- | :---- | :---- |
| 1 | 夜に開始した場合はその日に帰属する | ✓ |
| 2 | 日付をまたいだ深夜は前夜（開始した夜）に帰属する | ✓ |
| 3 | 4:59はまだ前夜に帰属する | ✓ |
| 4 | 5:00ちょうどからは当日に帰属する（サイクルの切れ目） | ✓ |
| 5 | 月をまたぐ深夜でも前日へ正しく戻る | ✓ |
| 6 | 年をまたぐ深夜でも前日へ正しく戻る | ✓ |
| 7 | うるう年の3/1深夜は2/29に帰属する | ✓ |
| 8 | 昼間は「これから始まる夜」として当日に帰属する（暫定仕様） | ✓ |

#### isNightTime（夜間帯の判定・要件2.3）

| No | 期待する挙動 | 結果 |
| :---- | :---- | :---- |
| 9 | 18:00ちょうどは夜間帯（開始できる） | ✓ |
| 10 | 深夜は夜間帯 | ✓ |
| 11 | 4:59は夜間帯 | ✓ |
| 12 | 5:00ちょうどは夜間帯外（自動終了の時刻） | ✓ |
| 13 | 昼は夜間帯外 | ✓ |
| 14 | 17:59は夜間帯外 | ✓ |

#### formatDateKey

| No | 期待する挙動 | 結果 |
| :---- | :---- | :---- |
| 15 | 端末ローカル基準で YYYY-MM-DD に整形する（月日は0埋め） | ✓ |

#### clampNonNegativeSeconds（端末時刻の変更への耐性・要件3.2）

| No | 期待する挙動 | 結果 |
| :---- | :---- | :---- |
| 16 | 負の差分は0に丸める | ✓ |
| 17 | 0以上はそのまま返す | ✓ |

#### formatMinutes（学習時間の表示）

| No | 期待する挙動 | 結果 |
| :---- | :---- | :---- |
| 18 | 0分 → 0分 | ✓ |
| 19 | 45分 → 45分 | ✓ |
| 20 | 60分 → 1時間 | ✓ |
| 21 | 95分 → 1時間35分 | ✓ |
| 22 | 720分 → 12時間 | ✓ |

**小計**: 22件（成功 22 / 失敗 0）

### 4.2 タイマー計測（時刻差分方式）

- **対象**: `src/lib/timer.ts`
- **関連する仕様**: 要件3.1（タイマー設定）／要件3.2（タイマー計測）
- **テストファイル**: `src/lib/__tests__/timer.test.ts`

**なぜ検証するのか**: 経過時間をカウンター変数ではなく時刻の差分で算出する方式のため、「一時停止」「5:00到達」「端末時計の変更」の組み合わせで壊れやすい。実績学習時間は成長・目標達成の判定に直結し、画面を見ても正しさが分からない。

#### getElapsedSeconds（時刻差分方式の経過秒）

| No | 期待する挙動 | 結果 |
| :---- | :---- | :---- |
| 1 | 一時停止していなければ、開始からの経過がそのまま進む | ✓ |
| 2 | 一時停止して再開した分は経過から除かれる | ✓ |
| 3 | 一時停止中は経過が進まない | ✓ |
| 4 | 端末時計を開始前へ巻き戻しても負にならず0になる（要件3.2） | ✓ |
| 5 | 一時停止の累積が経過を上回っても0で下げ止まる | ✓ |

#### getAutoEndMs / isAutoEndReached（5:00自動終了・要件3.2）

| No | 期待する挙動 | 結果 |
| :---- | :---- | :---- |
| 6 | 夜に開始したセッションは翌5:00で自動終了する | ✓ |
| 7 | 深夜開始でも同じ夜の5:00に終了する（学習日が前日のため） | ✓ |
| 8 | 月末に開始しても翌月1日の5:00へ正しく繰り上がる | ✓ |
| 9 | 5:00ちょうどで到達と判定する | ✓ |

#### getActualStudySeconds（実績学習時間・黙々モード）

| No | 期待する挙動 | 結果 |
| :---- | :---- | :---- |
| 10 | 予定時間を超えても計測は続く（予定は目標であって上限ではない） | ✓ |
| 11 | 5:00を過ぎた分は実績に含めない（実績は5:00まで） | ✓ |
| 12 | 一時停止中に5:00を過ぎた場合も、停止分を除いた5:00までが実績になる | ✓ |

#### getPomodoroPhase（フェーズ算出・要件3.1）

| No | 期待する挙動 | 結果 |
| :---- | :---- | :---- |
| 13 | 開始直後は1ループ目の作業フェーズ | ✓ |
| 14 | 作業25分を終えると休憩フェーズへ移る | ✓ |
| 15 | 休憩を終えると最後の作業フェーズへ移る | ✓ |
| 16 | 最後の作業の完了をもって全ループ完了となる（計55分） | ✓ |
| 17 | 最後の作業の後に休憩フェーズが現れない | ✓ |
| 18 | 繰り返し1回では休憩が一度も発生しない | ✓ |
| 19 | 4ループでは休憩が3回だけ挟まる（計115分） | ✓ |

#### getActualStudySeconds（実績学習時間・ポモドーロ）

| No | 期待する挙動 | 結果 |
| :---- | :---- | :---- |
| 20 | 休憩フェーズは実績に含めない | ✓ |
| 21 | 休憩を挟んだ後の作業は実績に積み上がる | ✓ |
| 22 | 全ループ完了後は作業時間の合計で頭打ちになる | ✓ |

#### getActualStudyMinutes（実績1分未満の破棄判定・要件3.2）

| No | 期待する挙動 | 結果 |
| :---- | :---- | :---- |
| 23 | 59秒は0分（保存せず破棄する） | ✓ |
| 24 | 60秒ちょうどから1分（保存する） | ✓ |

#### getPlannedMinutes（予定学習時間・要件3.2）

| No | 期待する挙動 | 結果 |
| :---- | :---- | :---- |
| 25 | 黙々モードは設定した予定時間 | ✓ |
| 26 | ポモドーロは作業時間×繰り返し回数（休憩は含めない） | ✓ |

#### shouldAutoFinish（自動終了の判定）

| No | 期待する挙動 | 結果 |
| :---- | :---- | :---- |
| 27 | 黙々モードは5:00到達でのみ自動終了する | ✓ |
| 28 | ポモドーロは全ループ完了でも自動終了する | ✓ |
| 29 | 一時停止中に5:00へ到達した場合も自動終了する（要件3.2） | ✓ |

#### getEndMs（記録する終了時刻）

| No | 期待する挙動 | 結果 |
| :---- | :---- | :---- |
| 30 | 通常の終了操作では、その時刻を終了時刻とする | ✓ |
| 31 | 5:00を過ぎていれば5:00を終了時刻とする | ✓ |
| 32 | ポモドーロ完了後に終了した場合、完了した瞬間を終了時刻とする | ✓ |
| 33 | 一時停止を挟んだポモドーロでは、停止分だけ完了時刻が後ろへずれる | ✓ |

**小計**: 33件（成功 33 / 失敗 0）

### 4.3 入力値の検証

- **対象**: `src/lib/validation.ts`
- **関連する仕様**: 要件1.2／3.1／3.4／5.2／12章
- **テストファイル**: `src/lib/__tests__/validation.test.ts`

**なぜ検証するのか**: 値域はドキュメント（要件定義書）とスキーマのCHECK制約が正。境界値（ちょうど・1つ外側）を固定し、実装がドキュメントからずれたことを検出する。

#### validateNickname（必須・20文字以内）

| No | 期待する挙動 | 結果 |
| :---- | :---- | :---- |
| 1 | 空・空白のみは弾く | ✓ |
| 2 | 20文字ちょうどは通り、21文字は弾く | ✓ |

#### validateDailyGoalMinutes（10〜720分）

| No | 期待する挙動 | 結果 |
| :---- | :---- | :---- |
| 3 | 9分は値域外 | ✓ |
| 4 | 10分ちょうどは通る | ✓ |
| 5 | 720分ちょうどは通る | ✓ |
| 6 | 721分は値域外 | ✓ |
| 7 | 未入力・数字以外は弾く | ✓ |

#### validatePlannedMinutes（黙々モードの予定学習時間・10〜660分）

| No | 期待する挙動 | 結果 |
| :---- | :---- | :---- |
| 8 | 9分 → 通る:false | ✓ |
| 9 | 10分 → 通る:true | ✓ |
| 10 | 660分 → 通る:true | ✓ |
| 11 | 661分 → 通る:false | ✓ |

#### validatePomodoro*（作業5〜120分 / 休憩1〜30分 / 繰り返し1〜10回）

| No | 期待する挙動 | 結果 |
| :---- | :---- | :---- |
| 12 | 作業時間の境界 | ✓ |
| 13 | 休憩時間の境界 | ✓ |
| 14 | 繰り返し回数の境界 | ✓ |
| 15 | 繰り返し回数のメッセージは「分」ではなく「回」で伝える | ✓ |

#### validateExtensionMinutes（延長宣言・5〜120分）

| No | 期待する挙動 | 結果 |
| :---- | :---- | :---- |
| 16 | 4分 → 通る:false | ✓ |
| 17 | 5分 → 通る:true | ✓ |
| 18 | 120分 → 通る:true | ✓ |
| 19 | 121分 → 通る:false | ✓ |

#### validateTagName（必須・20文字以内）

| No | 期待する挙動 | 結果 |
| :---- | :---- | :---- |
| 20 | 空は弾く | ✓ |
| 21 | 20文字ちょうどは通り、21文字は弾く | ✓ |

#### validateMemo（任意・500文字以内）

| No | 期待する挙動 | 結果 |
| :---- | :---- | :---- |
| 22 | 空は通る（任意項目のため） | ✓ |
| 23 | 500文字ちょうどは通り、501文字は弾く | ✓ |

#### validateNotificationTime（17:30〜翌4:30・要件12章）

| No | 期待する挙動 | 結果 |
| :---- | :---- | :---- |
| 24 | 形式が HH:MM でなければ弾く | ✓ |
| 25 | 17:29は範囲外 | ✓ |
| 26 | 17:30ちょうどは通る（夜の準備を促す通知） | ✓ |
| 27 | 21:00は通る | ✓ |
| 28 | 23:59は通る | ✓ |
| 29 | 00:00は通る（日付をまたぐ） | ✓ |
| 30 | 04:30ちょうどは通る | ✓ |
| 31 | 04:31は範囲外（5:00の自動終了に配慮） | ✓ |
| 32 | 12:00は範囲外（タイマーを開始できない時間） | ✓ |

**小計**: 32件（成功 32 / 失敗 0）

## 5. 実行結果サマリ

| 項目 | 件数 |
| :---- | :---- |
| テストケース合計 | 87 |
| 成功 | 87 |
| 失敗 | 0 |

**すべてのテストに成功している。**

## 6. 制約

- **UIの自動テストは持たない。** 画面の表示・レイアウト・操作感は実機での目視確認による
- **DBアクセスの自動テストは持たない。** スキーマのCHECK制約やSQLの挙動は、sqlite3で実際にDBを作成して都度検証している（マイグレーション・一時停止の累積・マイタグの重複と復活など）
- テストは端末のローカルタイムゾーンを前提とする（学習日の判定がローカル基準のため）
